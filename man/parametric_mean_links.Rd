% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplexreg_links.R
\name{parametric_mean_links}
\alias{parametric_mean_links}
\alias{parametric_mean_link}
\alias{parametric_mean_link_inv}
\alias{parametric_mean_link_deriv1}
\alias{parametric_mean_link_inv_deriv1}
\alias{parametric_mean_link_deriv2}
\title{Parametric Mean Link Functions and Derivatives}
\usage{
parametric_mean_link(mu, lambda, link_type = c("plogit1", "plogit2"))

parametric_mean_link_inv(eta, lambda, link_type = c("plogit1", "plogit2"))

parametric_mean_link_deriv1(mu, lambda, link_type = c("plogit1", "plogit2"))

parametric_mean_link_inv_deriv1(
  eta,
  lambda,
  link_type = c("plogit1", "plogit2")
)

parametric_mean_link_deriv2(mu, lambda, link_type = c("plogit1", "plogit2"))
}
\arguments{
\item{mu}{Mean parameter (numeric vector, 0 < mu < 1)}

\item{lambda}{Power parameter (numeric scalar, positive)}

\item{link_type}{Type of link function: \code{"plogit1"} or \code{"plogit2"}}

\item{eta}{Linear predictor of mean (numeric vector)}
}
\value{
Numeric vector with transformed values
}
\description{
Provides the parametric mean link functions, their inverses, and derivatives
for the simplex regression model. Two parametric link types are supported:
\code{"plogit1"} and \code{"plogit2"}.
}
\details{
Two parametric link functions are available:
\itemize{
  \item \strong{plogit2}: \eqn{g(\mu; \lambda) = \log(\mu^\lambda / (1 - \mu^\lambda))}
  \item \strong{plogit1}: \eqn{g(\mu; \lambda) = \log((1-\mu)^{-\lambda} - 1)}
}
Their inverses and derivatives with respect to \eqn{\mu} are also implemented.
}
\examples{
parametric_mean_link(0.2, lambda = 1.2, link_type = "plogit2")
parametric_mean_link(c(0.2, 0.5, 0.8), lambda = 1.5, link_type = "plogit1")
parametric_mean_link_inv(0, lambda = 1, link_type = "plogit2")
parametric_mean_link_deriv1(0.5, lambda = 1, link_type = "plogit2")
parametric_mean_link_inv_deriv1(0, lambda = 1, link_type = "plogit2")
parametric_mean_link_deriv2(0.5, lambda = 1, link_type = "plogit2")

}
